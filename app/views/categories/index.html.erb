<div id="name">

</div>
 @categories.each do |category| %>
  <h2 class="bold"><%= link_to category.name, category_path(category.slug) %></h2>
  <span><%= category.total_items %> total items.</span>
<% end %>
