<div class="col-md-4 table text-center">
  <% @categories.each do |category| %>
    <h2><%= link_to "#{category.name} (#{category.total_items} total items)", category_path(category.slug), id: 'underlined', class: 'bold' %></h2>
  <% end %>
</div>
